public class User {
    private String fullName;
    private String userID;




/*
    public static boolean validUserID (String userID) {
        if (userID)

        return true;
    }

    public void setUserID(String userID, String fullName) {
        String[] nameSplit = fullName.split(" ");
        String twoLettersFirstname = twoLetters(nameSplit, 0);
        String twoLettersLastname = twoLetters(nameSplit, 1);

        this.userID = userID;
    }
    public static String twoLetters (String[] nameSplit, int index) {
        String firstLetter = Character.toString(nameSplit[index].charAt(0));
        String secondLetter = Character.toString(nameSplit[index].charAt(1));
        String twoLetters = firstLetter + secondLetter;


        return twoLetters;
    }

 */
}
